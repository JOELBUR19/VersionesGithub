//No pude hacerlo por teclado, ya que investigue y se necesitan modulos, como no los conozco prefiero esperar a una explicación.

const numero = 4; 

if (numero % 2 === 0) {
  console.log("El número " + numero + " es un número par");
} else {
  console.log("El número " + numero + " es un número impar");
}